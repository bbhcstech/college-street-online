<!doctype html><html><head><meta charset="utf-8"><title>Invoice INV-P{{ $publisher->id }}-{{ $order->id }}</title><style>body{font-family:Arial,sans-serif;color:#16283e;margin:36px}.top,.summary{display:flex;justify-content:space-between;gap:20px}.brand{font-size:24px;font-weight:800}.muted{color:#6b7788}.status{display:inline-block;padding:6px 10px;border-radius:99px;background:#e8edf3;font-weight:700}.verified{background:#e3f6ed;color:#087c53}table{width:100%;border-collapse:collapse;margin:28px 0}th,td{text-align:left;padding:11px;border-bottom:1px solid #dbe2eb}th{background:#f1f5f9}.totals{margin-left:auto;width:330px}.totals div{display:flex;justify-content:space-between;padding:9px 0}.totals .net{border-top:2px solid #172f49;font-size:18px;font-weight:800}.notice{margin-top:28px;padding:14px;background:#fff8e9;border:1px solid #f0d49e;border-radius:8px;font-size:13px}@media print{button{display:none}body{margin:18px}}</style></head><body>
<button onclick="window.print()">Print / Save PDF</button><div class="top"><div><div class="brand">College Street Online</div><p class="muted">Publisher sales invoice</p></div><div><h2>INV-P{{ $publisher->id }}-{{ $order->id }}</h2><p>Order #CSO{{ $order->id }} · {{ $order->created_at->format('d M Y') }}</p></div></div>
<div class="summary"><div><strong>Publisher</strong><p>{{ $publisher->name ?? auth()->user()->name }}<br>{{ auth()->user()->email }}</p></div><div><strong>Customer</strong><p>{{ $order->customer?->name ?? '—' }}<br>{{ $order->customer?->email }}</p></div><div><strong>Payment</strong><p><span class="status {{ $order->payment?->verified_status==='verified'?'verified':'' }}">{{ ucfirst($order->payment?->verified_status ?? 'No payment') }}</span><br>@if($order->payment?->utr_number)UTR: {{ $order->payment->utr_number }}@endif</p></div></div>
<table><thead><tr><th>Book</th><th>Qty</th><th>Unit price</th><th>Gross</th><th>Rate</th><th>Deduction</th><th>Net</th></tr></thead><tbody>@foreach($items as $item) @php($itemGross=$item->quantity*($item->base_unit_price ?? $item->unit_price))<tr><td>{{ $item->book?->title ?? 'Book unavailable' }}</td><td>{{ $item->quantity }}</td><td>₹{{ number_format($item->base_unit_price ?? $item->unit_price,2) }}</td><td>₹{{ number_format($itemGross,2) }}</td><td>{{ number_format($item->publisher_commission_rate ?? 0,2) }}%</td><td>₹{{ number_format($item->publisher_commission_amount,2) }}</td><td>₹{{ number_format($itemGross-$item->publisher_commission_amount,2) }}</td></tr>@endforeach</tbody></table>
<div class="totals"><div><span>Gross book sales</span><b>₹{{ number_format($gross,2) }}</b></div><div><span>Publisher deductions</span><b>₹{{ number_format($deductions,2) }}</b></div><div class="net"><span>Net publisher amount</span><b>₹{{ number_format($net,2) }}</b></div></div>
<div class="notice"><strong>Manual payment record:</strong> This invoice records the publisher's portion of the order. It is not a payment-gateway settlement receipt. Payment verification is based on the UTR proof reviewed by the administrator.</div>
</body></html>
